========================== CI-CD Concept

                                                        |
                                                        |
                  BUILD ================== TEST ===================    PUSH TO
                   ||                                   |         DOCKER - HUB/AWS-ECR
                   ||                                   |                  ||
                   ||             CI                    |    CD            ||
                   ||                                   |                  ||
                   ||                                   |                  ||
                  CODE ========================================= DEPLOYE ON EKS/ECS 
                                                        |           ON-PREM
                                                        |




===================== Jenkins work-flow and installation setup
. Setup and installation
. Jenkins UI flow
. Jenkins Pipeline
. Automate CICD
. Agents/Nodes
. DevSecOps
. RBAC


=============================== Installation =============================
 
                          - Dedicated VM/EC2
 
Jenkins installation way  - Docker

                          - Kubernetes


Note: Jenkins requires Java to run, so need to install Java
==== Java Installation
sudo apt update
sudo apt install fontconfig openjdk-21-jre
java -version

=== Jenkins Installation

Long Term Support release:-

sudo wget -O /etc/apt/keyrings/jenkins-keyring.asc \
  https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key
echo "deb [signed-by=/etc/apt/keyrings/jenkins-keyring.asc]" \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null

sudo apt update
sudo apt install jenkins

Note: Jenkins will installed after this in your EC2 Machine, Jenkins run on 8080 port so go to Security Group in AWS and
Create ports, 
 unlock your jenkins :
 Go to this path cat /var/lib/jenkins/secrets/initialadminpassword

. http:://0.0.0.0/8080 jenkins work on that ports(as per your EC2 port).


============================ Pipeline Creating


. Discard old builds: 
This determines when, if ever, build records for this project should be discarded. Build records include the console output, archived artifacts,
and any other metadata related to a particular build. Keeping fewer builds means less disk space will be used in the Build Record Root Directory ,
which is specified on the Configure System screen.

   Jenkins offers two options for determining when builds should be discarded:

   - Build age: discard builds when they reach a certain age; for example, seven days old.
   - Build count: discard the oldest build when a certain number of builds already exist.

   Log rotation Strategy: discard old build by log ratation strategy  if jenkins taking much space.
 

. Do not allow concurrent builds:
  "Means one controller that check which job should run or not"
  "disableResume()" — a Jenkins pipeline option that stops the build from resuming after a controller restart, instead marking it as failed/aborted.

 A Jenkinsfile/pipeline option (options { disableResume() }) that prevents a running pipeline from resuming after the Jenkins controller restarts, 
 forcing it to fail or be re-triggered instead of picking up where it left off.
 

. Pipeline speed/durability override: 
  It's a setting that lets you choose between a faster pipeline or a safer pipeline — safer means it can survive a Jenkins restart, 
  faster means it skips that safety to run quicker.
                                                    or
 This setting allows users to change the default durability mode for running Pipelines. 
 In most cases this is a trade-off between performance and the ability for running pipelines to resume after unplanned Jenkins outages.

. Preserve stashes from completed builds: 
  A setting that keeps temporary files (stashes) from old, finished builds instead of deleting them right away 
  — normally Jenkins clears them out to save space, but this option holds onto them longer.

. Throttle builds:
  A setting that limits how many builds can run at the same time (or how fast they run), so the system doesn't get overloaded.

. Triggers:
Settings that tell Jenkins when to automatically start a build — like on a schedule, when code changes, 
 or after another job finishes — instead of someone starting it manually.
    - Build after other projects are built – Runs this job automatically after a specified job finishes.
    - Build periodically – Runs this job on a fixed schedule (like cron), regardless of code changes.
    - GitHub hook trigger for GITScm polling – Runs this job instantly when GitHub sends a push notification (webhook).
    - Poll SCM – Checks the repository at set intervals and runs the job only if new changes are found.
    - Trigger builds remotely (e.g., from scripts) – Lets an external script or tool start this job using a special URL/token.


======================== Creating the Pipelines


                                       Pipelile
                                        ||
           ---------------------------- ||----------------------------   
          |                                                          |
   Declarative Pipeline                                            Static pipeline 
     (Grovvy Based)


===================== > Sample Pipeline

    pipeline{
    agent any;
    stages{
        stage("pull/clone form Github"){
            steps{
               echo "pulling form Github source code"
            }
        }
        stage("teting"){
            steps{
            echo "unit testing done"
            }
        }
        stage("build from dockerfile"){
            steps{
               echo "docker file buile done"
            }
        }
        stage("Log in to AWS ECS "){
            steps{
            echo "ECS Log in done"
            }
        }
        stage("push Image to docker hub"){
            steps{
            echo "pushing image done on docker hub"
            }
        }
        stage("Deploye on EKS Cluster"){
            steps{
            echo "Deployment done on EKS Cluster"
            }
        }
        
    }
}


======================> Pipeline - 1

pipeline{
    agent any;
    stages{
        stage("pull/clone form Github"){
            steps{
               git url: "https://github.com/SachinDubey0307/two-tier-flask-app.git",branch: "master"
            }
        }
        stage("teting"){
           steps{
               echo "Unit testing done"
            }
        }
        stage("build from dockerfile"){
               steps{
             sh "docker build -t flaskapp:latest ."
             echo "you have to add current logged in user and jenkins user to docker group due to docker permission issue"
             echo "sudo usermod -aG docker ubuntu"
             echo "sudo usermod -aG docker jenkins"
             echo "sudo systemctl restart jenkins"
            }
        }
        stage("Log in to AWS ECS "){
            steps{
            echo "ECS Log in done"
            }
        }
        stage("push Image to docker hub"){
            steps{
            echo "pushing image done on docker hub"
            }
        }
        stage("Deploye on EKS Cluster"){
            steps{
            echo "sudo apt-get install docker-compose-v2"
            echo "docker compose version"
            sh "docker compose up -d --build flask-app"                                  #this will build your compose everytime( --build flask-app)
            }
        }
    }
}


Note: If Ownership issue come use this
sudo chown -R jenkins mysql-data/



                ======================  Automate CICD   =================================

                        ================ Push to docker hub/ECR ============
                       |                                                    |
                       |                                                    |
Git VCS ========= Docker Image                                          docker-compose use that ==========  Run App    
  |                                                                             image                          |
  |                                                                                                            |
  ============================================================================================================|               

                                              Automation - only need web hook now



Steps:

 1. Configure Docker hub CRED in Jenkins Go to Dashboard
Manage Jenkins ======>> Credentails ======>> System ========>> (Global) =======>> Global Credentails (unrestricted)  === Add Credentials====

Username with password  ===== 

Username      -    sachindubey03 (docker hub username)
password      -    Go to Docker hub A/c == Account settings == PAT == Create PAT === Genrate  === Keep that password on Jenkins
id            -    dockerhubcreds
Descreption   - this is docker hub cred

Note : Now you have Docker hub access in Jenkins, you can put image in docker hub and deploye by docker hub


======================> Pipeline - 2

. Go to Jenkins pipeline and configure docker hub Credential in pipeline

Remember: withCredentials([usernamePassword()]){} 
          "withCredentials allow you to use multiple credentials in jenkins pipeline like usernamePassword, ssh etc"

Pipeline: 

pipeline{
    agent any;
    stages{
        stage("pull/clone form Github"){
            steps{
               git url: "https://github.com/SachinDubey0307/two-tier-flask-app.git",branch: "master"
            }
        }
        stage("teting"){
           steps{
               echo "Unit testing done"
            }
        }
        stage("build from dockerfile"){
               steps{
             sh "docker build -t flaskapp:latest ."
             echo "you have to add current logged in user and jenkins user to docker group due to docker permission issue"
             echo "sudo usermod -aG docker ubuntu"
             echo "sudo usermod -aG docker jenkins"
             echo "sudo systemctl restart jenkins"
            }
        }
        stage("push Image to Docker Hub"){
    steps{
        withCredentials([usernamePassword(
            credentialsId: "dockerhubcreds",
            usernameVariable: "dockerHubUser",
            passwordVariable: "dockerHubPass"
        )]){
            sh "docker login -u ${env.dockerHubUser} -p ${env.dockerHubPass}"
            sh "docker image tag flaskapp:latest ${env.dockerHubUser}/flaskapp:latest"
            sh "docker push ${env.dockerHubUser}/flaskapp:latest"
        }
    }
}
        stage("Deploye on EKS Cluster/Docker Compose"){
            steps{
            echo "sudo apt-get install docker-compose-v2"
            echo "docker compose version"
            sh "docker compose up -d --build flask-app"
            }
        }
    }
}

Note: 
Remember in Docker-compose.yaml

- Before docker cred configuration: 
flask-app:
    build:
      context: .
    container_name: flask-app

- After docker Cred configuration:

flask-app:
    image: sachindubey03/flaskapp:latest
    container_name: flask-app




=============================================  Continous Deployment (Automate pipeline)  ============================


Note: Integrate Jenkins with Github by GitHub - WebHook service


     =================== Push Code on Github/Git ============== 
    |                                                           |
    |                                                           |
    |                                                           |
 Devloper                                                      Jenkins ========== Deployedment


Weebhooks: 
Webhooks allow external services to be notified when certain events happen. When the specified events happen,
we'll send a POST request to each of the URLs you provide. Learn more in our


Step:

. Go to Githubrepo - settings
. Go to your Repository and go to its settings   ===== Webhooks   === Add webhook ===
   - keep your jenkins jobs url to Payload URL : http://15.207.111.159:8080/github-webhook/
   - Content type:  application/x-www-form-urlencoded
   - SSL verification: Disabled  due to not useing https
   - Which events would you like to trigger this webhook: Send me everything
   - Add webhook in Github , remember its always be Green tick as added (that means Last delivery was successful.)
.  Go to your EC2 Instance Security Rules and make it accessible to all IPV4.
. Goto your specifice jenkins jobs 
   Goto Jenkins=== Democicd or jobs === configure  === Trigger  ===  GitHub hook trigger for GITScm polling  === Save the jobs







======================================================= pipeline versioned controlling =========


. Everytime you have to come jenkins for pipeline changes so make Repo "Jenkinsfile" in Github repository
  and keep your jenkins pipeline in it. (completaly versioned controlled now) .

. now now need to go jenkins for pipeline changes so :
  - Go to Job configuration === change pipeline to pipeline script form SCM  === 
       . SCM = Git
       . Repository: 
            Repository URL :                       https://github.com/SachinDubey0307/two-tier-flask-app
            Credential:                            if private repo / none for public
            Branch Specifier (blank for 'any') :   */master
            Script Path:                           Jenkinsfile

. Save the pipeline.
            
 




























































