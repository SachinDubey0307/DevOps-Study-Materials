
=====================================  Terraform Architecture and SetUp  ====================================



Introduction to Terraform.
. What is Terraform.
. why Infrastracture as code matters.
. Terraform Lifecycle and workflow.
. Terraform and Ansible.
. Terraform and CloudFormation.
. Terraforma and OpenTofu.
. Installation and Setup.
. install on local machine , AWS EC2.
. EC2 CLI Setup and Credentails.
. Verfying installation.
. Folder stracture in HCL Basics
. Block , Argument and Attributes
. Block (Resource,Provider,Varible,Output,Data block)
. Terraform Workflow ( terraform init, terraform validate, terraform plan, terraform apply, terraform destroy)
. Reading and understand terrafrom  output 
- '-refresh only' concept


==================== What is Terraform 

                         ->  Valuts 
                              
- Invented by Hashicorp  ->  Boundary (VPN)

                        ->  Terraform (IAC)


- Till 2023 Terraform are Opensource 
(2024 decide as Business source Licence)
(Before Business source its fork as open source as 'Open Tofu')

- Use terraform Documentation for best learning.



===================> why Infrastracture as code matters.
Terraform is an IAC tool that Its Automate the infrastrature management
(like Provision of instances, storage backup and Database, network administration, whole process for infrastrature making and its reduce the:
- Slow deployment             - Expence            - Limited Automation
- Human Error                 - Wastd Resource     - Inconsistancy



=================> Terraform and Ansible.
Terraform is Infrastarture Provision or Declrative way of  tool and Ansible is Infrastrature Management tool
(Imprative tool that work on step by step)



================ > Configuration Terraform on EC2

. Configure EC2 Instance on AWS and update the server.
. Go to Terraform Documentation for installasation setup

. Linux Setup and configuration:
wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep -oP '(?<=UBUNTU_CODENAME=).*' /etc/os-release || lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform


. terrafrom --version: To validate the installation



  ======================= HCL Basics Structure


 Remember:

     Block  Perametre {
          Arguments (before declaration)
                  or 
          Attributes (after declaration)
     }

 Blcok : Resource Block, Varible Block, Provider Block, output block.
 Perametre: Local_name, AWS_name, AZURE_name, GCP_name. 
 Argument and Attributes: its are the configuration detail.


================================= Creating HCL configuration for creating sample_file, AWS S3 Bucket and AWS EC2 Instance ===========

================== CREATING Sample_File and Automate by Terraform:


mkdir terraform-practice
           |||

cd terraform-practice

           |||

vim main.tf (for creating sample file for resouce creation in terraform)

            |||
# sample file creation

resource local_file myfile{
  filename = "my_file.txt"
  content = "hello world, this terraform sample file" 
}

           |||


terraform init ( for initialzation terraform in repository with resouce )


          |||

terraform validate


          |||


terraform Plan


         |||


terraform apply

        |||

terraform destroy (if want to destroy the Infra)
        |||

terraform destroy -traget=local_file.myfile (Note: only specific resouce deletion or destroy)




================== CREATING S3_Bucket and Automate by Terraform:

. Prerequisite: 

        Terraform =========== connect by aws cli ============== s3 bucket

    1. So you have to Configure and install AWS CLi to ec2 instance.(aws cli v2 install)
      . Run the followning command
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip awscliv2.zip
            sudo ./aws/install
                  ||
            aws --version

    2. configure aws ami user in aws cli and  provide required permission.
      . Go to iam role and create user and give attach access policy.
      . Go to iam security Cred and create access key.
      . Go to ubuntu console and run command 'aws configure'
      . configure accesskey and secrete access key.
      . Ready to use aws by CLI.
      . aws s3 ls 

   3. Now go to cli and go to terraform file and create s3.tf file
     . Go to Terraform Registry and search configuration for s3
     . sample file creation :

      # sample file creation
           provider aws {
                       region = "ap-south-1"
                        }
           resource aws_s3_bucket mybuckets
                       {
                       bucket = "mybucketterraaa"
                tags = {
                       Name = "my -bucket"
                       Environment = "dev"
                       }
                       }
  4. terraform init: for configure aws provider
                  ||
  5. terrafrom validate
                  ||
  6. terraform plan
                  ||
  7. terraform apply


================== CREATING EC2_Instance and Automate by Terraform:

 . Manual process for creating EC2 Instance:

    1. Region
    2. Key-value crating (public-private)
    3. VPC (subnetting and others)
    4. Security Group (inbound/outbound rules,, Note: prefer vpc security group)
    5. OS (AMI)  (Note: that is create inside instance creation)
    6. Instance Name
    7. Instance Type
    8. Storage (EBS, and disk size,, search in terraform registry - "root block" )
    

. Prerequisite for Automate Ec2 instance by Terraform:
1. create file ec2.tf 
       vim ec2.tf
2. 
#AWS Region

provider "aws" {
    region = "ap-south-1"
}

#Key-Value pair

resource "aws_key_pair" "my_key_pair" {
  key_name   = "terra-automate-key"
   public_key = file("terra-automate-key.pub")
  #public_key = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBwm6bJ5wjKpdBgR+e4g6siBiVRxBZ2CMHcjldOD2IiK ubuntu@ip-172-31-5-210"
}


#VPC Cration Default

resource "aws_default_vpc" "default" {
  tags = {
    Name = "Default VPC"
  }
}


# Security Group Creation, prefer vpc security group

resource "aws_security_group" "my_security_group" {
  name        = "terra-security-group"
  description = "Allow TLS inbound traffic and all outbound traffic"
  vpc_id      = aws_default_vpc.default.id

  tags = {
    Name = "allow_tls"
  }
}

# Inbound and outbount ports rules

resource "aws_vpc_security_group_ingress_rule" "allow_tls_ipv4" {
  security_group_id = aws_security_group.my_security_group.id
  cidr_ipv4         = "0.0.0.0/0"
  from_port         = 80
  ip_protocol       = "tcp"
  to_port           = 80
}

# Allowing inbound and outbound rule for ssh

resource "aws_vpc_security_group_ingress_rule" "allow_ssh" {
  security_group_id = aws_security_group.my_security_group.id
  cidr_ipv4         = "0.0.0.0/0"
  from_port         = 22
  ip_protocol       = "tcp"
  to_port           = 22
}

resource "aws_vpc_security_group_ingress_rule" "allow_tls_ipv6" {
  security_group_id = aws_security_group.my_security_group.id
  cidr_ipv6         = "::/0"
  from_port         = 80
  ip_protocol       = "tcp"
  to_port           = 80
}

resource "aws_vpc_security_group_egress_rule" "allow_all_traffic_ipv4" {
  security_group_id = aws_security_group.my_security_group.id
  cidr_ipv4         = "0.0.0.0/0"
  ip_protocol       = "-1" # semantically equivalent to all ports
}

resource "aws_vpc_security_group_egress_rule" "allow_all_traffic_ipv6" {
  security_group_id = aws_security_group.my_security_group.id
  cidr_ipv6         = "::/0"
  ip_protocol       = "-1" # semantically equivalent to all ports
}


#EC2- Instance -  Creation 

resource "aws_instance" "my_instance" {
  #count         = 5
  ami           = "ami-01a00762f46d584a1"                                              # Ami- ID
  instance_type = "t3.micro"                                                           # Instance-type
  vpc_security_group_ids = [aws_security_group.my_security_group.id]                   # vpc & security group
  key_name = aws_key_pair.my_key_pair.key_name                                       # key-pair
                                                                                       # root-block-storage or EBS
  root_block_device {
                   volume_size = 10
                   volume_type = "gp3"
  }

  tags = {
    Name = "HelloWorld"                                                                # Instance Name
  }
}


                    |||
             terraform validate
                    |||
              terraform plan
                    |||
              terraform apply -auto -approve


Remember: Give Permission by AWS IAM as per requirement.






  
   
  
     



              
           





































